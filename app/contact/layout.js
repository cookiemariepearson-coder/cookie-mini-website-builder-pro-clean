export const metadata = {
  title: 'Contact and Support',
  description: 'Contact Cookie Digital Creations for Mini Website Builder support.',
  alternates: { canonical: '/contact' }
};

export default function ContactLayout({ children }) {
  return children;
}
