import { notFound } from 'next/navigation';import SitePreview from '../../../lib/SitePreview';import { getSupabaseAdmin } from '../../../lib/supabaseAdmin';
export const dynamic='force-dynamic';
function fallback(row){return row.site||{businessName:row.business_name||'Published Website',customerEmail:row.customer_email||'',plan:row.plan||'starter',headline:row.headline||'A beautiful website created in minutes.',description:row.description||'',primaryColor:row.primaryColor||'#20172f',accentColor:row.accentColor||'#c46a2d',typeKey:'local',pages:row.pages||['Home','Services','Contact'],offerTitle:'Services & Offers',offers:[{title:'Main Service',text:'Describe your service.'},{title:'Highlights',text:'Share what makes it special.'},{title:'Contact',text:'Tell people how to reach you.'}],sections:{}}}
export default async function Page({params}){const supabase=getSupabaseAdmin();const{data,error}=await supabase.from('websites').select('*').eq('slug',params.slug).maybeSingle();if(error||!data||data.status==='paused')notFound();const site=fallback(data);return <SitePreview site={{...site,slug:params.slug}} live/>}
