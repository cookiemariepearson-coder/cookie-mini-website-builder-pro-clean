'use client';
import { useState } from 'react';import Nav from '../../lib/Nav';
export default function Customer(){const[email,setEmail]=useState('');const[slug,setSlug]=useState('');const[msg,setMsg]=useState('');async function go(){if(!slug){setMsg('Enter your website name/subdomain.');return;}location.href=`/customer/edit/${slug.trim().toLowerCase()}?email=${encodeURIComponent(email)}`;}return <><Nav/><main className="wrap dashboard"><h1>Customer Dashboard</h1><p>Enter your email and website name/subdomain to edit your website.</p><div className="field"><label>Email</label><input value={email} onChange={e=>setEmail(e.target.value)}/></div><div className="field"><label>Website name/subdomain</label><input placeholder="my-business-name" value={slug} onChange={e=>setSlug(e.target.value)}/></div>{msg&&<div className="notice error">{msg}</div>}<button className="btn" onClick={go}>Open My Website</button></main></>}
