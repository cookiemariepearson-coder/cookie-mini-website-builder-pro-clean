import { NextResponse } from 'next/server';import { getSupabaseAdmin } from '../../../../lib/supabaseAdmin';
export async function POST(req){try{const {pin,slug,updates}=await req.json(); if(pin!==process.env.ADMIN_PIN) return NextResponse.json({ok:false,error:'Invalid PIN'},{status:401}); const safe={}; ['status','plan','extra_pages','monthly_price','customer_email','business_name'].forEach(k=>{if(updates?.[k]!==undefined)safe[k]=updates[k]}); safe.updated_at=new Date().toISOString(); const supabase=getSupabaseAdmin(); const {error}=await supabase.from('websites').update(safe).eq('slug',slug); if(error) throw error; return NextResponse.json({ok:true});}catch(e){return NextResponse.json({ok:false,error:e.message},{status:500})}}
