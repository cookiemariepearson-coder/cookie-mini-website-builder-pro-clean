export const metadata = {
  title: 'AI Video Studio — $5 One-Time',
  description: 'Get video planning tools and one real AI-generated video with a verified $5 one-time purchase.',
  alternates: { canonical: '/checkout/ai-video' },
  robots: { index: true, follow: true }
};

export default function AiVideoCheckoutLayout({ children }) {
  return children;
}
