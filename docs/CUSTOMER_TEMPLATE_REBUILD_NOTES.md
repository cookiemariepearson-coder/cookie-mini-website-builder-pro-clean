# Template Rebuild Notes

The fix replaces the generic preview with a new type-aware template system.

The new templates are intentionally purple/gold branded, but still use different icons, labels, tags, and layout feel for different business types.

This keeps the new branding while making the customer websites feel more polished and more useful.
